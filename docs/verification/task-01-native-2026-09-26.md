# TASK-01 run history qualification, 26 September 2026

## Scope and prerequisites

This slice explains existing schedule outcomes and displays absolute run times
in Home Assistant's configured time zone. It uses the existing HA scheduler,
Bridge store and administrator boundary. Conversational editing, pause and
cancellation remain open in issue #112.

The existing Node/Python tools, Docker and authorised managed HAOS-DEV were
available. No new hardware, provider credential, host grant or production
schedule was needed. DEV and production VMs were already running.

## Local verification

The panel passed lint, 506 unit tests, all 90 browser cases, build and generated
consistency checks. Desktop and narrow-screen screenshots were inspected.
Cases cover every public status, safe unknown-state text, unavailable dates,
HA time-zone fallback, daylight-saving labels and a browser in another zone.
The complete non-root Linux Bridge suite passed: 2,195 tests and 27 skips.
All eight cold-restore ownership cases passed separately as root. The staged
amd64 App image built; its non-root immutable-package check rejected overlapping
worker leases and permitted reuse after release. Reverse-proxy transport checks
passed all route profiles and their disposable resources were removed.

During review, a real HTTP server returning successful headers then closing
mid-body reproduced a stopped event consumer. The Integration now classifies
transport interruptions as retryable at its canonical HTTP reader. The
regression verifies replay from the unchanged cursor and one HA accepted-event
delivery. Oversized bodies, malformed JSON and invalid event batches still
stop without retry. Independent review found no remaining issue.

The complete HA suite including the four recovery cases passed 417 tests. Windows policy/updater
checks passed 96 tests with one unavailable-symlink skip. Ruff, compile,
release/lock validation and candidate hassfest passed. Local HACS passed its
nine checks against published main; candidate HACS remains a CI gate.

## Native DEV evidence

The managed 1.8.3 backend recorded two due occurrences with one dispatched
turn and one overlap skip. No second turn started. Cancellation and cleanup
passed. A fresh browser-closed repeat produced the same result.

For the missed-window check, Core and its observer stayed running while only
the App was stopped before the first due time. The App restarted more than
five minutes after that due time. Native HA reconciliation, without an
Integration reload or manual claim, recorded exactly one missed-window skip
and advanced the next due time into the future. It dispatched no missed turn
and produced no catch-up storm or duplicate claim.

Both final fixtures cleaned up with zero errors, restoring the five original
idle chats, no schedules and the original effective options. Notifications
stayed off. Supervisor is healthy/supported with no resolution issues. DEV
retains three backups after the superseded Bridge backup was removed.

Paired 1.8.4 publication and managed target acceptance remain pending. A cold
DEV rollback backup exists; it has not been restore-tested. Production remains
1.8.2. The native backend evidence above is for managed 1.8.3; the candidate
panel and interrupted-response correction have separate local evidence.
