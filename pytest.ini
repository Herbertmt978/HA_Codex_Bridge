[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath =
    .
    bridge_service/src
testpaths =
    tests
addopts =
    --strict-config
    --strict-markers
timeout = 30
